RESEARCH_PROVIDER=mock
DATABASE_URL=file:./dev.db
HERMES_API_URL=
HERMES_API_KEY=
# Optional local CLI/subprocess adapter. Example:
# HERMES_COMMAND=hermes
HERMES_COMMAND=

# SQLite is configured at prisma/dev.db for the local demo.
# No database credentials are required.
